angular
.module("BYOBlanket")
.controller("reservationController",
    function ($scope, $location, $routeParams, napSpaceFactory, $timeout) {
    // add event listened on id="reserveButton"

    // get values from date, startTime and endTime, stored in variables startDateTime and endDateTime

    // use the makeReservation to PUT startDateTime and endDateTime on the unavailable property

    // send email to napSpace owner telling them a user has made a reservation, the owner can send the user an approve or decline

    })